# Public Image Candidate Archive

Archived on 2026-06-05 during production-readiness P1-9.

These files were removed from `public/images/` so they are no longer copied into
the static export. They are design candidates, generated drafts, original source
inputs, or currently unreferenced media. Runtime-referenced production assets
remain in `public/images/`.

The selected mobile hero candidate was copied to:

- `public/images/hero-mobile.jpg`

The production CSS now references that stable path instead of the old candidate
folder path.
