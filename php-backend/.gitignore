# Echte Konfiguration mit Passwort/Secret darf NICHT ins Repo.
# Vorlage ist config.example.php (committed).
config.php

# Beim Upload automatisch erzeugte Metadaten
pdf/*.json

# Event-Anfrage-Logs (enthalten personenbezogene Daten, gehoeren NICHT ins Repo)
logs/

